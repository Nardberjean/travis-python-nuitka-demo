language: python
python:
  - 3.6
  - 3.6-dev
  - nightly
matrix:
  fast_finish: true
  allow_failures:
    - python: nightly
script:
  - pytest
  - nuitka --recurse-all --recurse-stdlib --exe Hello_world.py
  - Hello_world.exe
